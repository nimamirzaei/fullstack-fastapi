apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: {{ .Values.backend.port }}
  type: ClusterIP
